[package]
name = "nexlink-taos"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
serde = { workspace = true, features = ["derive"] }
time = { workspace = true, features = ["serde", "formatting", "macros"] }
taos = { version = "0.12.3", default-features = true }
