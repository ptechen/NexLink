# Copy this file to .env or export variables manually before running deploy.sh

# Secret used by nexlink-relay to derive proxy credentials
NEXLINK_CREDENTIALS_SECRET=replace-with-a-long-random-secret
